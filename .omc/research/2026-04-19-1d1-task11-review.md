# 1D.1 Task 11 Post-mortem — Independent Review

**Date**: 2026-04-19
**Reviewer**: worker-gamma (Wave 2 Phase 1a-REVIEW role)
**Author under review**: worker-beta (post-mortem at `.omc/research/2026-04-19-1d1-task11-postmortem.md`)
**Plan**: `/home/midasdf/suzume/.omc/plans/2026-04-19-wpt-100-wave2.md` §Phase 1a-REVIEW
**Base**: `wave2-base` = `4cf67ed`

---

## VERDICT: APPROVE

Worker-delta MAY proceed with Phase 1b (polyfill deletion retry #3) **subject to the hard gates in §Conditions below**. If any gate fails post-deletion, Task 11 is dropped from Wave 2 per plan §Phase 1a drop-path.

Anti-self-approval compliance: worker-gamma (reviewer) ≠ worker-beta (author) ≠ worker-delta (executor). Task #3 owner confirmed = `worker-beta` via `TaskList` at review start.

---

## Evidence — Section (a) spot-check

**Claim under test**: All 11 Element-attribute methods the polyfill monkey-patched have native entry points at `wave2-base`, and the 33 failing subtests' failure signatures are consistent with polyfill-sidecar artefacts (not native-missing-functionality).

**Reviewer verification steps**:

1. **Re-ran baseline measurement** (independent of beta's `/tmp/attributes-baseline.txt`):
   ```
   cd /tmp/wpt && python3 -m http.server 9876 &
   timeout 90 /home/midasdf/suzume/zig-out/bin/suzume \
       --wpt-mode "http://127.0.0.1:9876/dom/nodes/attributes.html" \
       2>&1 | grep -E "^WPT_(SUMMARY|TEST)" > /tmp/gamma-spotcheck.txt
   ```
   Result: `WPT_SUMMARY: PASS=34 FAIL=33 TOTAL=67` ✅ matches beta's baseline exactly.

2. **Re-confirmed beta's 33-failure subtest list** via `head -60 /tmp/attributes-baseline.txt`: every failure signature from row 1-33 of beta's table (a) matches the live `WPT_FAIL:` lines byte-for-byte. Failure-class breakdown (value/localName/name/ownerElement/length N vs M) matches §(a) failure-signature clustering table.

3. **Spot-check 3 native entry points** via `Grep` against `src/js/kotori_dom.zig`:

   | Method | Claimed | Verified |
   |---|---|---|
   | `nativeSetAttribute` | :3305 | ✅ `fn nativeSetAttribute` at :3305 |
   | `nativeSetAttributeNS` | :3345 | ✅ at :3345 |
   | `nativeGetAttribute` | :3384 | ✅ at :3384 |
   | `nativeRemoveAttribute` | :3399 | ✅ at :3399 |
   | `getOrCreateAttrWrapper` | :4492 | ✅ at :4492 |
   | `setAttrOwnerElement` | (Task 10) | ✅ at :4744 |
   | `nativeGetAttributeNode` | :4939 | ✅ at :4939 |
   | `nativeGetAttributeNodeNS` | :4952 | ✅ at :4952 |
   | `nativeHasAttributeNS` | :4972 | ✅ at :4972 |
   | `nativeGetAttributeNS` | :4989 | ✅ at :4989 |
   | `nativeRemoveAttributeNS` | :5021 | ✅ at :5021 |
   | `nativeSetAttributeNodeImpl` | :5119 | ✅ at :5119 |
   | `nativeRemoveAttributeNode` | :5244 | ✅ at :5244 |
   | `nativeHasAttribute` | :6544 | ✅ at :6544 |
   | `nativeToggleAttribute` | :6565 | ✅ at :6565 |
   | `refreshAttributesMap` | (Task 8 cde4e5f) | ✅ at :5589 |

   All **15** cited line numbers are **accurate** against `wave2-base` HEAD. **11/11** Element.prototype methods registered via ep@763-794 in `kotori_dom.zig` (registration lines confirmed: 777, 780, 783, 784, 786, 789, 794).

4. **Deep-read 3 natives** (spot-check specific spec invariants):
   - `nativeSetAttributeNodeImpl:5119-5234` — InUseAttributeError (:5143-5157), idempotence (:5189-5191), re-key logic (:5209-5213) ALL present exactly as claimed.
   - `nativeRemoveAttributeNode:5244-5286` — NotFoundError via backing-ptr walk (:5269-5286) present as claimed.
   - `getOrCreateAttrWrapper:4492-4561` — correctly populates `name/localName/nodeName/nodeType/specified/value/nodeValue/textContent`. **Confirms beta's flagged residual risk** (§(c) wrapper-fields class): `namespaceURI` and `prefix` are hardcoded `null` at :4531-4532, validating beta's ⚠ NS-aware materialisation concern.

5. **Verified polyfill install sites**: `kotori_runtime.zig:92` (`self.eval(attributes_polyfill_js)`) + `:1499` (source constant) both present at `wave2-base` as claimed.

**Conclusion for (a)**: claims match reality, line numbers are accurate, failure signatures match the polyfill-sidecar narrative. ✅ PASS.

---

## Evidence — Section (b) independence check

**Claim under test**: Task 9 revert (`7c31ab6`) and Task 11 revert (`1853a09`) are addressed as DISTINCT incidents with DISTINCT root causes.

**Reviewer verification**:

1. **Both revert commits exist** (`git log --format="%H %s" -1`):
   - `7c31ab6772781a9...` = `Revert "feat(kotori): delete attributes polyfill (Layer 1D Task 9)"` ✅
   - `1853a09f9071965...` = `Revert "feat(kotori): delete attributes polyfill — native coverage complete (1D.1 Task 11)"` ✅
   - Distinct SHAs, distinct original commits (`7aee668` vs `709e617`), distinct messages.

2. **Post-mortem structure**: §(b) is split into TWO clearly-labelled sub-sections:
   - §(b) "Revert 1 — `7c31ab6`" (L113-149): identifies root cause as **8 missing natives** (`getAttributeNode`, `getAttributeNodeNS`, `setAttributeNode`, `setAttributeNodeNS`, `removeAttributeNode`, `hasAttributeNS`, `getAttributeNS`, `removeAttributeNS`) missing at Task 9 HEAD. Cites 8 specific follow-up commits (`0c314d6`, `ba80007`, `2c69eeb`, `063746b`, `a46307e`, `8bd52ad`, `4490f8f`, `cde4e5f`) that landed AFTER the revert and close the gap.
   - §(b) "Revert 2 — `1853a09`" (L151-192): identifies root cause as **Attr-wrapper identity cross-path inconsistency** (DOMException identity/message divergence between polyfill-throw and native-throw paths), inferred from absence of halt-log. Explicitly marks status "PARTIALLY" closed and lists residual uncertainty.

3. **Distinct root causes confirmed**:
   - Root cause #1: **Missing natives** (concrete, commit-enumerable, **CLOSED**).
   - Root cause #2: **Identity/validation divergence under polyfill+native coexistence** (inferential, **PARTIALLY CLOSED**, flagged as residual uncertainty requiring empirical verification in Phase 1b).

   These are clearly different failure modes, addressed independently. The post-mortem does NOT collapse them into "one narrative"; it even notes Revert 2 lacks a halt-log that Revert 1 could provide indirectly via the commit message's own gap enumeration.

4. **All 8 follow-up commits verified on `main`** (`git log --all --oneline | grep ...`): `0c314d6, ba80007, 2c69eeb, 063746b, a46307e, 8bd52ad, 4490f8f, cde4e5f, 0e0d6f2` all exist as Task 1-10 of Layer 1D.1 and are ancestors of `wave2-base`.

**Conclusion for (b)**: two reverts treated as distinct incidents with distinct root-cause hypotheses. Root cause #1 is CLOSED with commit-level evidence; root cause #2 is PARTIALLY CLOSED with inferential evidence, which is acceptable given the reviewer-authored §Conditions below that gate the third attempt empirically. ✅ PASS.

---

## Evidence — Section (c) coverage floor check

**Claim under test**: ≥95% of currently-passing polyfill-dependent subtests have native coverage at `wave2-base`. Documented residuals allowed.

**Reviewer interpretation of plan W2-Q8**: The bar in practice is "no mass-regression of currently-passing subtests after deletion", measured by `attributes.html` pass count post-deletion ≥ 34 (the current baseline). The §(c) ≥95% gate is a **predictive bar** (what fraction of currently-passing OR currently-failing subtests have native routes), not a passing-subtest-only gate. Beta correctly frames this.

**Reviewer verification**:

1. **33/33 failing subtests have native entry points** (Table (a) Subtest map rows 1-33): every failing subtest maps to a named `native<Method>` fn with a verified line number. 100% coverage at the native-entry level.

2. **Residual risk scoped to 4 subtests** (subtests 17, 31, 32, 33):
   - Subtest 17 ("Attribute with prefix in local name") — `getOrCreateAttrWrapper:4531-4532` sets `namespaceURI=null, prefix=null`. **Reviewer-verified** in code read-through — this IS a real residual risk for NS-aware Attrs materialised from lexbor pointers (not just createAttributeNS path).
   - Subtests 31-33 ("Own property names should include all qnames for ...") — depend on `refreshAttributesMap` (Task 8 `cde4e5f`) handling HTML-doc lowercase + non-HTML-doc case-preservation. Beta flags this appropriately.

3. **Aggregate coverage**: 29/33 without residual-risk flag = 87.9% of failing subtests cleanly native-owned.
   - On the currently-**passing** 34 subtests: beta asserts all route through native code post-deletion (no new failures expected). Not independently re-verified per-subtest by reviewer (would require deletion trial); accepted as plausible given §(a) evidence.
   - Combined coverage on the **67-subtest surface**: 34 passing (all native-routed per beta) + 29 failing-with-native-route-no-residual-flag + 4 failing-with-native-route-residual-flagged = **100% native-routable** with 4 flagged subtests = **94% clean**.

4. **Margin vs 95% plan bar**: 94% clean is 1pp below the strict gate. **However**: the plan's W2-Q8 proposed resolution states "(c) is complete-coverage for every passing subtest at `wave2-base`" — and **all 34 passing subtests** are native-routed per beta's §(a) table. The 4 flagged subtests are **currently failing**, so they CANNOT regress the passing count; they can only stay failing or start passing. Under the zero-regression-of-passing interpretation, coverage is effectively 100% on the passing side.

5. **Commit hashes spot-checked for accuracy**: `cde4e5f` (Task 8), `063746b` (Task 4), `0e0d6f2` (Task 10) all verified via `git log --all --oneline | grep`. Citations are real.

**Conclusion for (c)**: 100% native-routable for failing subtests, 100% native-owned for passing subtests per beta's analysis, 4 documented residuals all on the currently-failing side (no regression risk on passing count). Meets the plan's ≥95% bar under the "complete-coverage for passing subtests" interpretation. ✅ PASS with caveat that Phase 1b MUST empirically validate the inference.

---

## Conditions (HARD GATES — worker-delta MUST enforce)

These are non-negotiable. If any gate fails post-deletion, worker-delta MUST revert + mark Task 11 dropped per plan.

### Gate D1 — `attributes.html` pass count floor
After polyfill deletion and rebuild, `./zig-out/bin/suzume --wpt-mode` on `attributes.html` MUST report `WPT_SUMMARY: PASS >= 34`. If `PASS < 34`, **HALT immediately**, revert, write `.omc/research/2026-04-19-task11-retry-halt.md` with full log. This is the third attempt; a third revert = Task 11 dropped from Wave 2 per plan §Phase 1a drop path.

### Gate D2 — `dom/nodes` area no-regression
Run `TIMEOUT=90 ./tests/wpt/run_wpt_parallel.sh --jobs 4 --port 9876 dom/nodes`. Total pass count MUST be ≥ Phase 0b baseline for `dom/nodes`. Specifically watch: `Element-setAttribute.html`, `Element-removeAttributeNS.html`, `Element-hasAttribute.html`, `Element-hasAttributes.html`, `Element-setAttribute-crbug-1138487.html`, `Document-createAttribute.html`, `attributes-namednodemap.html`, `MutationObserver-attributes.html`. Any file's pass count below its Phase 0b baseline → HALT + revert.

### Gate D3 — `html/dom` area no-regression
Run `TIMEOUT=90 ./tests/wpt/run_wpt_parallel.sh --jobs 4 --port 9877 html/dom`. Watch `reflection-embedded.html` specifically. Any regression → HALT + revert.

### Gate D4 — Binary size
Binary size delta ≤ −5 KB (expected ≈ −10-12 KB per beta's estimate of 333×35-byte lines). If > −5 KB (e.g., no shrink or grew), something is wrong (dead polyfill still linked?) — HALT, investigate, don't commit.

### Gate D5 — Build green
`zig build -Doptimize=ReleaseSafe` AND `zig build test` MUST both pass. If either fails → HALT, fix or revert.

### Gate D6 — Residual-risk subtest monitoring (advisory, not HALT)
Watch these 4 specifically in the post-deletion `attributes.html` run:
- #17 "Attribute with prefix in local name"
- #31 "Own property names should only include all-lowercase qnames for HTML elem/doc"
- #32 "Own property names should include all qnames for non-HTML elem in HTML doc"
- #33 "Own property names should include all qnames for HTML elem in non-HTML doc"

If any of these 4 **regress from their current state** (all are currently FAIL — regression here means a new fail signature or a crash, not staying failed), document in the retry log. Not a HALT trigger unless Gate D1 also trips.

---

## Protocol compliance

- ✅ Reviewer ≠ author: `worker-gamma` (me) ≠ `worker-beta` (post-mortem author). `TaskList` at review start showed Task #3 owned by `worker-beta` as expected.
- ✅ Reviewer ≠ Phase 1b executor: Phase 1b Task #5 owner will be `worker-delta` per plan.
- ✅ Adversarial scrutiny applied: re-ran independent baseline measurement, cross-referenced all 15 cited line numbers against `wave2-base`, verified both revert SHAs independently, deep-read 3 natives for invariant spot-check, verified 1 flagged residual-risk is real (subtest 17 namespaceURI/prefix null hardcode).
- ✅ No re-review loop: this verdict is binding. If worker-delta trips any HARD gate, Task 11 drops; no further review round permitted in Wave 2.

---

## Files referenced

- Post-mortem under review: `/home/midasdf/suzume/.omc/research/2026-04-19-1d1-task11-postmortem.md`
- Plan: `/home/midasdf/suzume/.omc/plans/2026-04-19-wpt-100-wave2.md` §Phase 1a-REVIEW
- Baseline raw log: `/tmp/attributes-baseline.txt` (PASS=34 FAIL=33 TOTAL=67)
- Reviewer-run baseline: `/tmp/gamma-spotcheck.txt` (PASS=34 FAIL=33 TOTAL=67 — matches)
- Native source: `/home/midasdf/suzume/src/js/kotori_dom.zig` at lines 3305, 3345, 3384, 3399, 4492, 4744, 4939, 4952, 4972, 4989, 5021, 5119, 5244, 5589, 6544, 6565
- Polyfill source: `/home/midasdf/suzume/src/js/kotori_runtime.zig:92, :1499`
- Revert commits: `7c31ab6772781a9`, `1853a09f9071965`
- Original delete commits: `7aee668152d403e`, `709e6177571a8e5`
- Follow-up natives (Task 1-10): `0c314d6`, `ba80007`, `2c69eeb`, `063746b`, `a46307e`, `8bd52ad`, `4490f8f`, `cde4e5f`, `0e0d6f2`

---

*End of review. Worker-delta is cleared to proceed with Phase 1b under HARD GATES D1-D5 (D6 advisory).*
